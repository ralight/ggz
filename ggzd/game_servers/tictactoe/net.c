/* Generated by GGZComm/ruby version 0.4 */
/* Protocol 'tictactoe', version '4' */
/* Do NOT edit this file! */

#include "net.h"
#include <stdlib.h>
#include <ggz.h>

#define ggz__seat_open 1

static notifier_func_type notifier_func = NULL;
static error_func_type error_func = NULL;
static int ret;
static int requirelink = 0;
static int nextlink;

static void ggzcomm_error(void);

static void ggzcomm_sndmove(GGZCommIO * io)
{
	ret = ggz_read_int(io->fd, &variables.move_c);
	if(ret < 0)
		ggzcomm_error();
	if(notifier_func)
		(notifier_func) (sndmove);
}

static void ggzcomm_reqsync(GGZCommIO * io)
{
	if(notifier_func)
		(notifier_func) (reqsync);
}

void ggzcomm_msgseat(GGZCommIO * io)
{
	ret = ggz_write_int(io->fd, msgseat);
	if(ret < 0)
		ggzcomm_error();
	ret = ggz_write_int(io->fd, variables.num);
	if(ret < 0)
		ggzcomm_error();
}

void ggzcomm_msgplayers(GGZCommIO * io)
{
	int i1;

	ret = ggz_write_int(io->fd, msgplayers);
	if(ret < 0)
		ggzcomm_error();
	for(i1 = 0; i1 < 2; i1++)
	{
		ret = ggz_write_int(io->fd, variables.seat[i1]);
		if(ret < 0)
			ggzcomm_error();
		if((variables.seat[i1] != ggz__seat_open))
		{
			ret = ggz_write_string(io->fd, variables.name[i1]);
			if(ret < 0)
				ggzcomm_error();
		}
	}
}

void ggzcomm_reqmove(GGZCommIO * io)
{
	ret = ggz_write_int(io->fd, reqmove);
	if(ret < 0)
		ggzcomm_error();
}

void ggzcomm_rspmove(GGZCommIO * io)
{
	ret = ggz_write_int(io->fd, rspmove);
	if(ret < 0)
		ggzcomm_error();
	ret = ggz_write_char(io->fd, variables.status);
	if(ret < 0)
		ggzcomm_error();
}

void ggzcomm_msgmove(GGZCommIO * io)
{
	ret = ggz_write_int(io->fd, msgmove);
	if(ret < 0)
		ggzcomm_error();
	ret = ggz_write_int(io->fd, variables.player);
	if(ret < 0)
		ggzcomm_error();
	ret = ggz_write_int(io->fd, variables.move);
	if(ret < 0)
		ggzcomm_error();
}

void ggzcomm_sndsync(GGZCommIO * io)
{
	int i1;

	ret = ggz_write_int(io->fd, sndsync);
	if(ret < 0)
		ggzcomm_error();
	ret = ggz_write_char(io->fd, variables.turn);
	if(ret < 0)
		ggzcomm_error();
	for(i1 = 0; i1 < 9; i1++)
	{
		ret = ggz_write_char(io->fd, variables.space[i1]);
		if(ret < 0)
			ggzcomm_error();
	}
}

void ggzcomm_msggameover(GGZCommIO * io)
{
	ret = ggz_write_int(io->fd, msggameover);
	if(ret < 0)
		ggzcomm_error();
	ret = ggz_write_char(io->fd, variables.winner);
	if(ret < 0)
		ggzcomm_error();
}

void ggzcomm_network_main(GGZCommIO * io)
{
	int opcode;

	ggz_read_int(io->fd, &opcode);

	if(requirelink)
	{
		if(opcode != nextlink)
			ggzcomm_error();
		requirelink = 0;
	}

	switch (opcode)
	{
		case sndmove:
			ggzcomm_sndmove(io);
			break;
		case reqsync:
			ggzcomm_reqsync(io);
			break;
	}
}

void ggzcomm_set_notifier_callback(notifier_func_type f)
{
	notifier_func = f;
}

void ggzcomm_set_error_callback(error_func_type f)
{
	error_func = f;
}

GGZCommIO *ggzcomm_io_allocate(int fd)
{
	GGZCommIO *io = malloc(sizeof(GGZCommIO));
	io->fd = fd;
	return io;
}

void ggzcomm_io_free(GGZCommIO * io)
{
	ggz_free(io);
}

static void ggzcomm_error(void)
{
	if(error_func)
		(error_func) ();
}
