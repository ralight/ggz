KDE_LANG = en
KDE_DOCS = muehle

EXTRA_DIST = index.docbook

uninstall-local:
	rm -f $(DESTDIR)$(prefix)/share/doc/HTML/en/muehle/index.cache.bz2

clean-local:
	rm -f $(top_builddir)/muehle/doc/index.cache.bz2

