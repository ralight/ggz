if COND_METASERV
METASERV_DIR = metaserv
endif
if COND_TELGGZ
TELGGZ_DIR = telggz
endif
if COND_CMDCLIENT
CMDCLIENT_DIR = cmd-client
endif
if COND_GGZCOMM
GGZCOMM_DIR = ggzcomm
endif

SUBDIRS = man $(METASERV_DIR) $(TELGGZ_DIR) $(CMDCLIENT_DIR) $(GGZCOMM_DIR)

EXTRA_DIST = README.GGZ \
		m4/ggz.m4 m4/iconv.m4
